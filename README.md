# Compressible Flow Calculator Web App

This is a React based web user interface for calculating compressible aerodynamics relations. It uses a [Java backend API](https://github.com/SierraOG/CompressibleFlowCalculator) for doing the calculations and uses React Router, Forms, and Hooks for implementing the single page web application. 